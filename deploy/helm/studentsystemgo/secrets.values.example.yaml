# Example secrets file. Copy to `secrets.values.yaml` and fill real values.
# DO NOT commit secrets.values.yaml — it's gitignored.

mysql:
  rootPassword: "CHANGE_ME"
  replication:
    password: "CHANGE_ME"

rabbitmq:
  defaultPass: "CHANGE_ME"

env:
  DB_PASSWORD: "CHANGE_ME"   # must match mysql.rootPassword (app connects AS root)
  JWT_SECRET: "CHANGE_ME"